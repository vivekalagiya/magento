<div class="page-title">
    <h1><?php echo $this->__('Create New Product') ?></h1>
</div>

<?php //$attributeData = $this->getAttribute()->getData(); 
?>

<form id="" method="post" action="<?php echo $this->getSaveUrl(); ?>">
    <button title="Reset" type="button" class="scalable button" onclick="setLocation('<?php echo $this->getResetUrl(); ?>')"><span><span><span>Reset</span></span></span></button>
    <button title="Save" type="submit" onclick="checkDetails()" class="scalable save button"><span><span><span>Save</span></span></span></button>
    <?php if (array_key_exists('product_id', $productData)) : ?>
        <button title="Delete" type="button" onclick="setLocation('<?php echo $this->getDeleteUrl(); ?>')" class="scalable save button"><span><span><span>Delete</span></span></span></button>
    <?php endif; ?></p><br>

    <div id="vendorProductForm">
    </div>
</form>


<style>
    .button1 {
        float: right;
    }
</style>

<script>

function checkDetails() {

    flag = 0;

    jQuery("input,select,textarea").each(function(){
        //alert(jQuery(this).attr("name")+' '+jQuery(this).attr("id"));
        if(jQuery(this).attr("id") == "required"){
            if(jQuery(this).val() == "" || jQuery(this).val() == undefined){
                ptag = jQuery(this).prev().children()[0];
                console.log(ptag);
                alert("Please Fill "+jQuery(ptag).text()+" value");
                flag = 1;
                return false;
            }
        }
    });
    if(flag){
        return false;
    }else{
        return true;
    // jQuery("#form1").submit();
    }
};

</script>